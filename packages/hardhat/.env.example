# Deployer private key for Sepolia and other live networks (64 hex chars, optional 0x).
# If missing or invalid, local Hardhat/Anvil use a built-in dev key; Sepolia has no signer (fine for verify-only).
DEPLOYER_PRIVATE_KEY=""

# Etherscan.io API key (string — enables API v2 for Sepolia verification). https://etherscan.io/apis
ETHERSCAN_API_KEY=""
